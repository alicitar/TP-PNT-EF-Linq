@model InstitutoIdioma.Models.Pregunta

@{
    ViewData["Title"] = "Detalle";
}

<h1>Detalle</h1>

<div>
    <h4>Pregunta</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Enunciado)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Enunciado)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" asp-route-examenid="@Model.Examen.Id">Editar</a> |
    <a href="javascript:void(0);" onclick="history.go(-1);">Volver</a>
</div>
